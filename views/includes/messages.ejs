
<% messages.forEach(message => { %>
    <% if(message.type === 'danger') { %>
        <div class="message message-danger"><%= message.text %></div>
    <% } else if(message.type === 'success') { %>
        <div class="message message-success"><%= message.text %></div>
    <% }%>
<% }) %>
